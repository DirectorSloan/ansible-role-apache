---

- name: enable httpd service and assure it is started
  service:
    name: '{{ apache_servicename }}'
    enabled: yes
    state: started
  tags:
    - apache
    - httpd
    - webserver
    - service

...
